import { styled } from "styled-components";

export const WorkflowPlaygroundContainer = styled.div`
  display: flex;
  gap: 12px;
  height: calc(100vh - 64px);
  padding: 40px;
  overflow: hidden;
`;
