import { Typography } from "antd";
import { styled } from "styled-components";
const { Text } = Typography;
export const TopbarHeadingText = styled(Text)`
  color: var(--Text-Color-900, #171717);
  font-size: 20px !important;
  font-style: normal;
  font-weight: 700;
  line-height: normal;
`;
